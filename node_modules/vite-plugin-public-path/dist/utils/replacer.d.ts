import { Expression, StringLiteral } from "@swc/core";
import { Visitor } from "@swc/core/Visitor";
export declare class PlaceholderReplacer extends Visitor {
    private readonly placeholder;
    private readonly expression;
    constructor(placeholder: string, expression: string);
    static parseExpression(expression: string): Expression;
    visitStringLiteral(node: StringLiteral): Expression;
}
export declare function replaceStringWithExpression(code: string, placeholder: string, expression: string, minify: boolean): Promise<string>;
