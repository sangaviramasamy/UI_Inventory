function getBlibliObject ({
  mockLoader
}) {
  return {
    mock: {
      update: mockLoader.updateMock,
      reset: mockLoader.resetMock,
      loader: mockLoader
    }
  }
}

module.exports = {
  getBlibliObject
}
