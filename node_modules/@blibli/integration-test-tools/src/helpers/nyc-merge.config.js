const { getPath } = require('../utils/path')
const { nyc: excludeConfig } = require('./blibli-integration-test.config')
const excludeFiles = excludeConfig?.exclude || []

module.exports = {
  exclude: [
    'integrations/**',
    ...excludeFiles
  ],
  reporter: [
    'json'
  ],
  'report-dir': getPath('integrations/.nyc_output_merge'),
  'temp-dir': getPath('integrations/.nyc_output')
}
