function _isMemberExpression (node) {
  return node.callee.type === 'MemberExpression'
}

function _isValidProp (node, obj, name) {
  if (!_isMemberExpression(node)) return

  const nodeName = obj.type === 'Identifier' ? obj.name : undefined
  return nodeName === name
}

function isObject (node, name) {
  return _isValidProp(node, node.callee.object, name)
}

function isCalleeProperty (node, name) {
  return _isValidProp(node, node.callee.property, name)
}

module.exports = {
  isObject,
  isCalleeProperty
}
