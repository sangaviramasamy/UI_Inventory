const { getPath } = require('../utils/path')

module.exports = {
  exclude: [
    'integrations/**'
  ],
  reporter: [
    'lcov',
    'text'
  ],
  'report-dir': getPath('integrations/coverage'),
  'temp-dir': getPath('integrations/.nyc_output_merge')
}
