/**
 * eslintrc configs
 */
const newEslintrcValue = `
module.exports = {
  env: {
    node: true
  },
}`

// env value
const envString = /env:\s*{/
const newEnv = `env: {
    'jest/globals': true,`

// eslint globals value
const moduleExportsString = /module\.exports\s*=\s*{/
const globalsConfig = `module.exports = {
  globals: {
    page: 'readonly',
    context: 'readonly',
    $blibli: 'readonly'
  },`

module.exports = {
  newEslintrcValue,
  envString,
  newEnv,
  moduleExportsString,
  globalsConfig
}
