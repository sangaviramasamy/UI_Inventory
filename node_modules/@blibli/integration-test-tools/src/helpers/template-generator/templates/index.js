const integration = require('./integration-scripts-template')
const testSpecs = require('./test-specs-template')
const git = require('./git-template')
const eslintrc = require('./eslintrc-template')
const babelrc = require('./babelrc-template')
const jenkinsfile = require('./jenkinsfile-template')
const sonarProject = require('./sonar-project-template')

module.exports = {
  integration,
  testSpecs,
  git,
  eslintrc,
  babelrc,
  jenkinsfile,
  sonarProject
}
