const { jestBase, jestBaseUpdateSnapshot, jestRun } = require('./base')
const { generateReport } = require('./report')
const { appsRun, appsRunLocal } = require('./apps')
const { doTest, doTestLocal } = require('./test')

module.exports = {
  jestBase,
  jestBaseUpdateSnapshot,
  generateReport,
  jestRun,
  appsRun,
  appsRunLocal,
  doTest,
  doTestLocal
}
