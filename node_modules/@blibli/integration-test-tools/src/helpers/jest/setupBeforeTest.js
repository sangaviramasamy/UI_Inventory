const utils = require('../../../lib/utils/animation')

beforeEach(() => {
  expect.hasAssertions()
})

// set stop animation as default behaviour
beforeAll(async () => {
  await utils.stopAnimations(page)
})