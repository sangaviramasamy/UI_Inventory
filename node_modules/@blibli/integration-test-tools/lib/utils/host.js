const { isDocker } = require('../../src/utils/container.js')
const { mainUi, app } = require('../../src/helpers/blibli-integration-test.config')

const name = isDocker ? 'localhost' : 'host.docker.internal'

const port = mainUi?.port || app?.port

const targetHost = port ? `${name}:${port}` : name

module.exports = {
  isDocker,
  name,
  targetHost
}
