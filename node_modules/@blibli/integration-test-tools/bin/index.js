#!/usr/bin/env node

// validator
const { validateCLIInput } = require('../src/utils/cli-validator')

async function run () {
  // script validate integration configs and CLIInput
  await validateCLIInput()

  const app = require('../src/index')
  app.run()
}

run()
