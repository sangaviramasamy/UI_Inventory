// swift-tools-version: 5.7
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let iOSBasePath = "dist/iOS"
let package = Package(
    name: "blue-design-tokens",
    products: [
        // Products define the executables and libraries a package produces, and make them visible to other packages.
        .executable(name: "tokengen", targets: ["tokengen"]),
        .library(name: "BLUE-SwiftUI", targets: ["BLUE-SwiftUI"])
    ],
    dependencies: [
        // Dependencies declare other packages that this package depends on.
        // .package(url: /* package url */, from: "1.0.0"),
    ],
    targets: [
        .target(name:"BLUE-SwiftUI", path: "\(iOSBasePath)/BLUE-SwiftUI"),
        .executableTarget( name: "tokengen", dependencies: [], path: "./tools/tokengen")
    ]
)
