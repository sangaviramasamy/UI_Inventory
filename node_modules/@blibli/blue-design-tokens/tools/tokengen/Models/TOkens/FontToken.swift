//
//  File.swift
//  
//
//  Created by Aldrian Kwantawijaya on 11/06/23.
//

import Foundation
/**
 Main purpose of this model is mainly to map which font file corresponds to which swift loadable identifier, because swift loads the font name by it's
 postScriptName property.
*/
struct FontToken: Hashable {
    let typeface: String // most likely blibli
    let fileName: String // font filename
    var postScriptName: String? // the font's postScriptName (find this out using some app)
    // These are optional
    var weight: Int? // should be 400,500,600, etc.
    var style: String? // Medium, Italic, Bold etc.
    var tokenName: String? // The variable name representing this font ( currently its font weight name )
}
